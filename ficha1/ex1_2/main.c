#include <stdio.h>
int main()
{
	// Exercicio 2
	int a = 1, b = 2, c = 3, d = 4, e = 5;

	int f = a + b + c + d + e;
	int g = -a - b - c - d - e;

	printf(" Valores : %d, %d, %d, %d, %d\n", e, d, c, b, a);
	printf(" O valor de F e: %d\n", f);
	printf(" O valor de G e: %d\n", g);

	// %d imprime variaveis do tipo inteiro (int)
}